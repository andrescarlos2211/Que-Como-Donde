<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Producto publicado exitosamente</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>  
  </head>
  <body>
    <% if (typeof successMessage !== 'undefined') { %>
      <script>
        Swal.fire({
            title: '¡Publicación exitosa!',
            icon: 'success',
            timer: 7000,
            timerProgressBar: true,
        }).then(() => {
            window.location.href = '/index'; // Redirecciona a otra ruta
        });
    </script>
    <% } %>
    
    <h1>Todo bien!</h1>
    <p>Producto publicado exitosamente.</p>
    <p>Ahora serás redirigido a la pagina principal</p>
  </body>
</html>

